"""
Scholarly-source retrieval for ResearchForge.

Clients for the Semantic Scholar Graph API, the arXiv API, and the Crossref
API. Each client is dependency-free (stdlib + requests) and returns the same
result shape the research agent already consumes, plus provenance labels and
persistent identifiers.
"""
