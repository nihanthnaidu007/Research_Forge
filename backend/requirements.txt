# ResearchForge — Python dependencies.
#
# The pinned lock is the single source of truth. This file simply includes it
# so `pip install -r backend/requirements.txt` installs exactly the locked
# versions everywhere (host, CI, Docker).
#
# To bump a dependency: update and re-pin requirements.lock (e.g. with
# `pip-compile` or `pip freeze`), never by editing this file.
-r requirements.lock
